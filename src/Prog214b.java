/* import java.util.*;
import java.io.*;

public class Prog214b {
    public static void main(String[] args) {
        try {
            Scanner input = new Scanner(new File("data/prog214b.dat"));
            int[] list = new int[1000];
            int count = 0;
            double tax = 0;
            
            while (input.hasNext()){
                list[count] = input.nextInt();
                gross = hours * rate
                if (gross < 100)
                  gross = gross;
                  else if ( gross >100 && < 150)
                  tax = .08;
              else if ( gross > 150 && <= 199)
                tax = 0.12;
              else
                tax = 0.15;
                count++;
              System.out.print(hours + rate + gross + (gross*tax))
            }
          
          
        }
    }
}

 */